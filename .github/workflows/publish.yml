name: Publish jvm-proto
on: push
jobs:
  publish:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@master
      - uses: ./.github/actions/publish
        with:
          args: generateProto